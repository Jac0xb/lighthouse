// Needed to compile program but tests are in ../tests directory
// This is because the tests use lighthaus + sdk as dependencies
// If the tests were in the lighthaus crate then there would be a circular dependency

pub mod lighthaus_tests {}
