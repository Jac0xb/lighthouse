// Needed to compile program but tests are in ../tests directory
// This is because the tests use lighthouse + sdk as dependencies
// If the tests were in the lighthouse crate then there would be a circular dependency

pub mod lighthouse_tests {}
